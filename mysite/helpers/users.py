
def verify_email(uid, token):
    """
    Verifies a user with matching uid,token

    Args:
        uid (str): users uid
        token (str): The email verification token

    Returns:
        bool: a boolean value representing success status
    """
    return True

